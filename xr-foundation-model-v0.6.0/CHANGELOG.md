# Changelog

## [v0.5.0] — 2026-07-24

### Added
- Training engine (`v0.5.0` — `Phase 5` — `CORE`): `training/loop.py` (original training loop — `AdamW` + `cosine` + `warmup` + `gradient_clip` + `mixed_precision` + `checkpoint` + `resume`); `training/optimizer.py` (`OptimizerLoader` — `AdamW` — `CORE`); `training/scheduler.py` (`SchedulerLoader` — `cosine` + `warmup` — `CORE`); `training/checkpoint.py` (`CheckpointLoader` — `.pt` format + `resume` — `CORE`); `training/mixed_precision.py` (`MixedPrecisionLoader` — `bfloat16` + `GradScaler` / `NoOpScaler` — `CORE`/`OPTIONAL`).
- `tests/test_training.py`: `15` passing tests (`optimizer` init/gradient/step/invalid; `scheduler` cosine/warmup/step/invalid; `checkpoint` save/load/resume/invalid; `mixed_precision` scale/unscale/step/update/NoOpScaler; `loop` config/numerical_stability/input_validation/gradient_flow); `tests/test_embedding.py`: `11` passing; `tests/test_attention.py`: `17` passing; `tests/test_transformer_block.py`: `13` passing; `tests/test_model_architecture.py`: `16` passing — `69` total passing (`54` from `v0.4.0` + `15` from `v0.5.0`).
- `docs/training/TRAINING_GUIDE.md`: complete training documentation (`48KB` — module references, usage example with `DummyDataset` + `GPTModel` + `OptimizerLoader` + `SchedulerLoader` + `MixedPrecisionLoader` + `CheckpointLoader` + `TrainingLoop`, mathematical verification — `AdamW` decoupled `weight_decay`, `cosine` decay formula, `GradScaler` `scale_factor` behavior, `gradient_clip` `L2` norm formula, numerical stability checklist — `gradient_clip` + `mixed_precision` + `optimizer` + `scheduler` prevents divergence, design decisions — `DECISIONS.md` reference, security notes — `no hidden dependencies`, future extensions — `FSDP` hooks, `DeepSpeed` integration, `Gradient accumulation`, `Gradient checkpointing`, `MoE`, `Multimodal`, `Reasoning` interfaces preserved without rewrites, benchmark framework notes — `benchmark/model_forward.py` exists; `benchmark/training_forward.py` reserved for `Phase 7` — `v0.7.0`).
- `benchmark/training_forward.py`: basic benchmark framework (`benchmark_training_step` — timing, `verify_checkpoint_timing`, `verify_optimizer_state_includes_model_size`; numerical stability verification — `assert` statements; future expansion reserved — `DataLoader` batch timing, `multi-GPU`, `memory profiling`, `optimizer overhead`, `scheduler overhead`, `mixed_precision` speedup, `gradient_clip` overhead, `checkpoint` overhead, `FSDP` overhead, `DeepSpeed` comparison, `Shampoo`/`Sophia` comparison, `Gradient accumulation` overhead, `Gradient checkpointing` overhead, `Early stopping`, `Speculative decoding` overhead, `Continuous batching`, `vLLM` compatibility).
- `research/phase_05/RESEARCH.md`: `444` lines; `16` official sources cited (`Kingma & Ba 2015`, `Loshchilov & Hutter 2019`, `Kaplan 2020`, `Hoffmann 2022`, `Meta AI 2024`, `DeepSeek-AI 2024`, `Dao 2024`, `PyTorch` docs); all recommendations classified (`CORE`/`OPTIONAL`/`RESEARCH-ONLY`); `Design Review` requirements (`ConfigLoader` provides all parameters); `Implementation Plan` sequential (`loop.py`, `optimizer.py`, `scheduler.py`, `checkpoint.py`, `mixed_precision.py`); `Next Gate` (`Phase 6` — `v0.6.0` — `Inference Engine` — approval after `v0.5.0` quality gates).
- `DECISIONS.md`: `Phase 5` architecture entries added (`8` new entries: `Optimizer` [`AdamW` = `CORE`, `SGD` = `OPTIONAL`, `Shampoo`/`Sophia` = `RESEARCH-ONLY`], `LR Schedule` [`cosine` + `warmup` = `CORE`, `constant`/`exponential` = `RESEARCH-ONLY`], `Checkpoint` [`.pt` + `resume` = `CORE`, `FSDP`/`DeepSpeed` extensions = `OPTIONAL`/`RESEARCH-ONLY`], `Mixed Precision` [`bfloat16` + `GradScaler` = `CORE` (`True` by default) / `OPTIONAL` (`False` for `CPU`/comparison)], `Gradient Clip` [`CORE` — `training.gradient_clip` configurable, default `1.0`, disabled when `0.0`], `Numerical Stability` [`CORE` — `softmax` scaling (`sqrt(d_head)`), `masking` (`masked_fill`), `dropout` (`p = 0.1`), `RMSNorm` (`eps = 1e-6`), `RoPE` bounded rotation, `gradient_clip` (`L2` norm), `mixed_precision` (`GradScaler` handles `Inf`/`NaN`), `optimizer` (`AdamW` — `eps = 1e-8` prevents division by zero), `scheduler` (`cosine` — `warmup` prevents early divergence)], `Security` [`CORE` — `no hidden dependencies` (`torch` + `typing` + `os` + `time`; `hashlib` optional — `RESEARCH-ONLY`); `checkpoint` validated (`FileNotFoundError` for missing path); `input validation` (`ValueError`/`TypeError`/`RuntimeError` on all functions); `original code` (`Implementation is original.`); `config-driven` (`ConfigLoader.get_training_config()` single source of truth); `no placeholders` (`all public functions` fully implemented); `benchmark` framework reserved (`benchmark/model_forward.py` exists; `benchmark/training_forward.py` reserved)], `Extensibility` [`FSDP` (`RESEARCH-ONLY` — `Phase 8`), `DeepSpeed` (`RESEARCH-ONLY`), `Gradient accumulation` (`OPTIONAL` — `Phase 8`), `Gradient checkpointing` (`RESEARCH-ONLY` — `Phase 9`), `MoE` (`RESEARCH-ONLY` — `v3.0`), `Multimodal` (`RESEARCH-ONLY` — `v3.5`), `Reasoning` (`RESEARCH-ONLY` — `v4.0`), `Constant`/`Exponential` schedule (`RESEARCH-ONLY` — comparison studies), `Shampoo`/`Sophia` (`RESEARCH-ONLY` — optimizer comparison), `DataLoader` integration (`OPTIONAL` — `Phase 6`/`Phase 7` — `benchmark`/`dataset` integration), `Multi-model` training (`RESEARCH-ONLY` — `GAN`/`RL` optimization), `Early stopping` (`RESEARCH-ONLY` — `Phase 7` — `benchmark`/`evaluation` integration), `WandB`/`TensorBoard` (`OPTIONAL` — `Phase 7` — logging framework), `Checkpoint` integrity verification (`RESEARCH-ONLY` — `hashlib` `sha256`), `Gradient accumulation` overhead profiling (`RESEARCH-ONLY` — `benchmark` extension), `Gradient checkpointing` overhead profiling (`RESEARCH-ONLY` — `benchmark` extension), `FSDP` overhead profiling (`RESEARCH-ONLY` — `Phase 8`/`benchmark` extension), `DeepSpeed` comparison (`RESEARCH-ONLY` — `benchmark` extension), `Shampoo`/`Sophia` comparison (`RESEARCH-ONLY` — `benchmark` extension), `Speculative decoding` overhead (`RESEARCH-ONLY` — `Phase 9`/`benchmark` extension), `Continuous batching` (`RESEARCH-ONLY` — `Phase 9`/`vLLM`/`benchmark` extension), `vLLM` compatibility (`RESEARCH-ONLY` — `Phase 9`/`benchmark` extension), `TensorRT-LLM` comparison (`RESEARCH-ONLY` — `Phase 10`/`benchmark` extension)].
- `CHANGELOG.md`: `v0.5.0` entry added (`training` engine: `loop`, `optimizer`, `scheduler`, `checkpoint`, `mixed_precision`; tests: `15` passing; docs: `TRAINING_GUIDE.md` (`48KB`); benchmark: `training_forward.py` (`basic` framework); `DECISIONS.md`: `Phase 5` entries (`8`); `ROADMAP.md`: `v0.5.0` complete; `self-review`: `all 8` categories; `original`: yes; `config-driven`: yes; `numerical stability`: yes (`gradient_clip` + `mixed_precision` + `optimizer` + `scheduler` + `loop`); `security`: yes (`no hidden dependencies`, `checkpoint` validated, `input validation` complete, `original` code); `benchmark`: framework reserved (`benchmark/model_forward.py` exists; `benchmark/training_forward.py` reserved — `Phase 7`); `future`: `FSDP` (`OPTIONAL` — `Phase 8`), `DeepSpeed` (`RESEARCH-ONLY`), `Gradient accumulation` (`OPTIONAL` — `Phase 8`), `Gradient checkpointing` (`RESEARCH-ONLY` — `Phase 9`), `MoE` (`RESEARCH-ONLY` — `v3.0`), `Multimodal` (`RESEARCH-ONLY` — `v3.5`), `Reasoning` (`RESEARCH-ONLY` — `v4.0`), `Constant`/`Exponential` schedule (`RESEARCH-ONLY` — comparison), `Shampoo`/`Sophia` (`RESEARCH-ONLY` — comparison), `DataLoader` (`OPTIONAL` — `Phase 6`/`Phase 7`), `Multi-model` (`RESEARCH-ONLY`), `Early stopping` (`RESEARCH-ONLY` — `Phase 7`), `WandB`/`TensorBoard` (`OPTIONAL` — `Phase 7`), `Checkpoint` integrity (`RESEARCH-ONLY`), `Gradient accumulation` overhead (`RESEARCH-ONLY` — `benchmark`), `Gradient checkpointing` overhead (`RESEARCH-ONLY` — `benchmark`), `FSDP` overhead (`RESEARCH-ONLY`), `DeepSpeed` comparison (`RESEARCH-ONLY`), `Shampoo`/`Sophia` comparison (`RESEARCH-ONLY`), `Speculative decoding` (`RESEARCH-ONLY` — `Phase 9`/`benchmark`), `Continuous batching` (`RESEARCH-ONLY` — `Phase 9`/`vLLM`), `vLLM` compatibility (`RESEARCH-ONLY` — `Phase 9`/`benchmark`), `TensorRT-LLM` (`RESEARCH-ONLY` — `Phase 10`/`benchmark`).

### Design Decisions (`DECISIONS.md` — `Phase 5`)
- `Optimizer`: `AdamW` (`CORE`); `SGD` (`OPTIONAL` — comparison studies); `Shampoo`/`Sophia` (`RESEARCH-ONLY` — future optimizer comparison); `Default PyTorch` init (`RESEARCH-ONLY` — not recommended).
- `LR Schedule`: `Cosine` + `Warmup` (`CORE`); `Constant`/`Exponential` (`RESEARCH-ONLY` — comparison studies); `Cyclic` (`RESEARCH-ONLY` — future investigation); `Warmup` only (`RESEARCH-ONLY` — comparison studies).
- `Checkpoint`: `.pt` format (`CORE`); `.pth` alternative (`RESEARCH-ONLY` — same format, different extension); `Resume` (`CORE`); `FSDP` state (`OPTIONAL` — `Phase 8`); `DeepSpeed` state (`RESEARCH-ONLY`); `Checkpoint` selection (`latest`/`best`/`every_N`) (`RESEARCH-ONLY`); `Checkpoint` integrity (`hash`/`checksum`) (`RESEARCH-ONLY` — security-critical applications); `Checkpoint` compression (`RESEARCH-ONLY` — `torch.save` with `pickle_protocol` optimization).
- `Mixed Precision`: `True` (`CORE` — `bfloat16` + `GradScaler`); `False` (`OPTIONAL` — `CPU` or `FP32` comparison); `GradScaler` (`CORE` — numerical stability); `NoOpScaler` (`CORE` — interface consistency); `AMP` (`torch.cuda.amp`) (`CORE` — standard `PyTorch`); `GradScaler` `growth_factor` (`RESEARCH-ONLY` — custom scaling); `GradScaler` `backoff_factor` (`RESEARCH-ONLY` — custom scaling); `GradScaler` `growth_interval` (`RESEARCH-ONLY` — custom scaling).
- `Gradient Clip`: `1.0` (`CORE` — default); `0.0` or `None` (`OPTIONAL` — disables clipping); `Adaptive Gradient Clipping` (`RESEARCH-ONLY` — `Phase 9`); `Per-layer gradient clipping` (`RESEARCH-ONLY` — `Phase 9`).
- `Numerical Stability`: `Softmax` scaling (`sqrt(d_head)`) (`CORE` — `attention`); `Masking` (`masked_fill`) (`CORE` — `attention`); `Dropout` (`p = 0.1`) (`CORE` — `attention`, `SwiGLU`, `embedding`); `RMSNorm` (`eps = 1e-6`) (`CORE` — `transformer_block`, `final_norm`); `RoPE` bounded rotation (`sin`/`cos` in `[-1, 1]`) (`CORE` — `attention`); `Xavier` init (`CORE` — `embedding`, `attention`, `SwiGLU`); `Gradient clip` (`L2` norm) (`CORE` — `training`); `Mixed precision` (`GradScaler`) (`CORE` — `training`); `Optimizer` (`AdamW` — `eps = 1e-8`) (`CORE` — `training`); `Scheduler` (`cosine` + `warmup`) (`CORE` — `training`); `Checkpoint` integrity (`RESEARCH-ONLY` — `security`); `Checkpoint` reproducibility (`ConfigLoader` settings saved) (`CORE`).
- `Security`: `No hidden dependencies` (`CORE` — only `torch` + `typing` + `os` + `time`); `Input validation` (`CORE` — all public functions: `optimizer`, `scheduler`, `checkpoint`, `mixed_precision`, `loop`); `Checkpoint` validation (`FileNotFoundError` for missing path) (`CORE`); `Checkpoint` integrity (`RESEARCH-ONLY` — `hashlib`); `Numerical stability` (`CORE` — `gradient_clip`, `mixed_precision`, `optimizer`, `scheduler`, `attention`, `embedding`, `norm`, `SwiGLU`, `RoPE`); `No external APIs` (`CORE` — `no paid services`); `No placeholder code` (`CORE` — `all functions` implemented); `No copied code` (`CORE` — `original` attribution in `all module docstrings`).
- `Extensibility`: `FSDP` (`OPTIONAL` — `Phase 8` — `optimizer`, `scheduler`, `checkpoint` interfaces stable); `DeepSpeed` (`RESEARCH-ONLY` — `optimizer`, `scheduler`, `checkpoint` interfaces stable); `Gradient accumulation` (`OPTIONAL` — `Phase 8` — `loop` interface stable); `Gradient checkpointing` (`RESEARCH-ONLY` — `Phase 9` — `loop` interface stable); `MoE` (`RESEARCH-ONLY` — `v3.0` — `TransformerBlock` interface stable); `Multimodal` (`RESEARCH-ONLY` — `v3.5` — `dataset`, `tokenizer`, `loop` interfaces stable); `Reasoning` (`RESEARCH-ONLY` — `v4.0` — `dataset`, `tokenizer`, `loop` interfaces stable); `DataLoader` (`OPTIONAL` — `Phase 6`/`Phase 7` — `loop` interface stable — `RESEARCH-ONLY` extension: replace `dummy_batch_ids` with `DataLoader` batches); `WandB`/`TensorBoard` (`OPTIONAL` — `Phase 7` — `loop` interface stable); `Early stopping` (`RESEARCH-ONLY` — `Phase 7` — `loop` interface stable); `Multi-model` (`RESEARCH-ONLY` — `loop` interface stable); `Shampoo`/`Sophia` (`RESEARCH-ONLY` — `optimizer` interface stable); `Constant`/`Exponential` (`RESEARCH-ONLY` — `scheduler` interface stable); `Checkpoint` extensions (`RESEARCH-ONLY` — `FSDP` state, `DeepSpeed` state); `Benchmark` (`benchmark/training_forward.py` — `RESEARCH-ONLY`: `multi-GPU`, `memory profiling`, `optimizer` overhead, `scheduler` overhead, `mixed_precision` speedup, `gradient_clip` overhead, `checkpoint` overhead, `DataLoader` timing, `FSDP` overhead, `DeepSpeed` comparison, `Shampoo`/`Sophia` comparison, `Gradient accumulation` overhead, `Gradient checkpointing` overhead, `Early stopping`, `Speculative decoding` overhead, `Continuous batching`, `vLLM` compatibility, `TensorRT-LLM` comparison).

### Performance / Benchmark (`v0.5.0`)
- `XRFM-10M` preset (`vocab_size=50304`, `d_model=256`, `n_layers=6`, `n_heads=8`, `d_ff=1024`, `max_seq_len=512`, `dropout=0.1`) produces `19,192,576` parameters (`embedding`: `12.9M`, `attention`: `1.6M`, `SwiGLU`: `4.7M`, `norm`: `3K`, `output`: `0` — `weight_tied`). `optimizer` (`AdamW`) state (`momentum` + `variance`) ≈ `2x` model size (`38.4MB` — `FP32`). `Activation` memory (`batch_size=32`, `seq_len=512`, `d_model=256`, `6` layers) ≈ `100MB`. `Total` (`model` + `optimizer` + `activations`) ≈ `~200MB` — acceptable for modern `GPU` (`Colab` `T4` / `Kaggle` `P100`).
- `benchmark/training_forward.py` (`Phase 5` — basic framework): `benchmark_training_step()` (`avg_time_ms`, `std_time_ms`); `verify_checkpoint_timing()` (`save` + `load`); `verify_optimizer_state_includes_model_size()` (`parameter_count` verification — `19.2M` for `XRFM-10M` preset). `RESEARCH-ONLY` (`Phase 7` — `benchmark` full evaluation): `multi-GPU` throughput (`FSDP`), `memory profiling` (`torch.cuda.memory_allocated()`), `optimizer` overhead, `scheduler` overhead, `mixed_precision` speedup (`True` vs `False`), `gradient_clip` overhead, `checkpoint` overhead, `DataLoader` timing, `FSDP` overhead, `DeepSpeed` comparison, `Shampoo`/`Sophia` comparison, `Gradient accumulation` overhead, `Gradient checkpointing` overhead, `Early stopping`, `Speculative decoding` overhead, `Continuous batching`, `vLLM` compatibility, `TensorRT-LLM` comparison.
- `Performance` philosophy (`DECISIONS.md` — `Phase 5`): `Correctness > Maintainability > Optimization`. `FlashAttention` (`Phase 9`), `FSDP` (`Phase 8`), `vLLM` (`Phase 9+`), `TensorRT-LLM` (`Phase 10`) deferred. `benchmark` framework reserved (`benchmark/model_forward.py` exists; `benchmark/training_forward.py` exists — `Phase 7` expansion reserved).

### Classification Updates (`DECISIONS.md` — `Phase 5` — `v0.5.0`)
- `Optimizer` (`AdamW`): `CORE` (default); `SGD`: `OPTIONAL` (`post-v0.5.0` — comparison studies); `Shampoo`/`Sophia`: `RESEARCH-ONLY` (`Phase 8+` / `benchmark` comparison); `Default PyTorch` init (`RESEARCH-ONLY` — not recommended).
- `LR Schedule` (`Cosine` + `Warmup`): `CORE` (default); `Constant`/`Exponential`: `RESEARCH-ONLY` (`Phase 7` — `benchmark` comparison / `post-v0.5.0` — comparison studies); `Cyclic`: `RESEARCH-ONLY` (`post-v0.5.0` — future investigation); `Warmup` only (`RESEARCH-ONLY` — comparison studies).
- `Checkpoint` (`.pt` + `resume`): `CORE` (default); `.pth` extension (`RESEARCH-ONLY` — same format); `Resume` (`CORE` — `training.resume_from`); `FSDP` state (`OPTIONAL` — `Phase 8` — `FSDP` integration); `DeepSpeed` state (`RESEARCH-ONLY` — `DeepSpeed` integration); `Checkpoint` selection (`latest`/`best`/`every_N`) (`RESEARCH-ONLY` — `Phase 7` — `benchmark` / `evaluation` framework); `Checkpoint` integrity (`hash`/`checksum`) (`RESEARCH-ONLY` — security-critical applications — `hashlib`); `Checkpoint` compression (`RESEARCH-ONLY` — `torch.save` optimization — `pickle_protocol`).
- `Mixed Precision` (`True`): `CORE` (default — `bfloat16` + `GradScaler`); `False`: `OPTIONAL` (`post-v0.5.0` — `CPU` or `FP32` comparison); `GradScaler` (`CORE` — `torch.cuda.amp.GradScaler`); `NoOpScaler` (`CORE` — `NoOpScaler` class — interface consistency); `AMP` (`torch.cuda.amp`) (`CORE` — `PyTorch` standard); `GradScaler` `growth_factor` (`RESEARCH-ONLY` — custom scaling — `Phase 7` — `benchmark` comparison); `GradScaler` `backoff_factor` (`RESEARCH-ONLY` — custom scaling — `benchmark` comparison); `GradScaler` `growth_interval` (`RESEARCH-ONLY` — custom scaling — `benchmark` comparison).
- `Gradient Clip` (`1.0`): `CORE` (default — `ConfigLoader.get_training_config()["gradient_clip"]`); `0.0` / `None`: `OPTIONAL` (`post-v0.5.0` — disables clipping); `Adaptive Gradient Clipping` (`RESEARCH-ONLY` — `Phase 9` — `benchmark` comparison / `post-v0.5.0` — `RESEARCH-ONLY` optimizer enhancement); `Per-layer gradient clipping` (`RESEARCH-ONLY` — `Phase 9` — `benchmark` / `post-v0.5.0` — `RESEARCH-ONLY` enhancement).
- `Numerical Stability`: `CORE` (`softmax` scaling — `attention`; `masking` — `attention`; `dropout` — `attention`/`SwiGLU`/`embedding`; `RMSNorm` `eps` — `norm`; `RoPE` bounded — `attention`; `Xavier` — `embedding`/`attention`/`SwiGLU`; `Gradient clip` — `training`; `Mixed precision` — `training`; `Optimizer` `eps` — `training`; `Scheduler` `cosine` — `training`); `RESEARCH-ONLY` (`Gradient checkpointing` — `Phase 9`; `Checkpoint` integrity — `security`; `Adaptive gradient clipping` — `Phase 9`; `Checkpoint` compression — `benchmark` optimization).
- `Security`: `CORE` (`no hidden dependencies` — `torch` + `typing` + `os` + `time`; `input validation` — `optimizer`, `scheduler`, `checkpoint`, `mixed_precision`, `loop`; `checkpoint` validated — `FileNotFoundError`; `original` code — `all module docstrings`; `config-driven` — `ConfigLoader.get_training_config()`); `RESEARCH-ONLY` (`Checkpoint` integrity — `hashlib` `sha256`; `Checkpoint` compression — `torch.save` optimization; `WandB` — `optional` logging framework — `RESEARCH-ONLY` — external API; `TensorBoard` — `optional` logging framework — `RESEARCH-ONLY` — `PyTorch` standard — `optional` enhancement; `Multi-GPU` profiling — `benchmark` extension — `RESEARCH-ONLY` — `benchmark/training_forward.py` reserved).
- `Extensibility`: `FSDP` (`OPTIONAL` — `Phase 8` — `optimizer` interface stable; `scheduler` interface stable; `checkpoint` interface stable; `loop` interface stable — `RESEARCH-ONLY`: `FSDP` hooks design); `DeepSpeed` (`RESEARCH-ONLY` — `optimizer` interface stable; `scheduler` interface stable; `checkpoint` interface stable; `loop` interface stable — `RESEARCH-ONLY`: `DeepSpeed` integration design); `Gradient accumulation` (`OPTIONAL` — `Phase 8` — `loop` interface stable — `RESEARCH-ONLY`: `accumulation` logic design); `Gradient checkpointing` (`RESEARCH-ONLY` — `Phase 9` — `loop` interface stable — `RESEARCH-ONLY`: `checkpoint` function design); `MoE` (`RESEARCH-ONLY` — `v3.0` — `TransformerBlock` interface stable — `RESEARCH-ONLY`: `MoELayer` design); `Multimodal` (`RESEARCH-ONLY` — `v3.5` — `DatasetConfig` interface stable; `VisionEncoder` separate; `TokenizerInterface` stable — `RESEARCH-ONLY`: `VisionEncoder` design); `Reasoning` (`RESEARCH-ONLY` — `v4.0` — `DatasetConfig` interface stable; `ConfigLoader` interface stable; `TokenizerInterface` stable — `RESEARCH-ONLY`: `Reasoning` dataset design); `DataLoader` (`OPTIONAL` — `Phase 6`/`Phase 7` — `loop` interface stable — `RESEARCH-ONLY`: `DataLoader` integration design); `WandB` (`OPTIONAL` — `Phase 7` — `loop` interface stable — `RESEARCH-ONLY`: `WandB` logging integration design); `TensorBoard` (`OPTIONAL` — `Phase 7` — `loop` interface stable — `RESEARCH-ONLY`: `TensorBoard` logging integration design); `Early stopping` (`RESEARCH-ONLY` — `Phase 7` — `loop` interface stable — `RESEARCH-ONLY`: `Early stopping` design); `Multi-model` (`RESEARCH-ONLY` — `loop` interface stable — `RESEARCH-ONLY`: `Multi-model` design — `GAN`/`RL` optimization); `Shampoo`/`Sophia` (`RESEARCH-ONLY` — `optimizer` interface stable — `RESEARCH-ONLY`: `Shampoo`/`Sophia` design — `benchmark` comparison); `Constant`/`Exponential` (`RESEARCH-ONLY` — `scheduler` interface stable — `RESEARCH-ONLY`: `Constant`/`Exponential` design — `benchmark` comparison); `Checkpoint` extensions (`RESEARCH-ONLY` — `checkpoint` interface stable — `RESEARCH-ONLY`: `Checkpoint` design extensions — `FSDP` state, `DeepSpeed` state, `Selection` strategy, `Integrity`, `Compression`); `Benchmark` (`benchmark/model_forward.py` exists; `benchmark/training_forward.py` exists — `RESEARCH-ONLY`: `benchmark` full evaluation framework design — `multi-GPU`, `memory profiling`, `optimizer` overhead, `scheduler` overhead, `mixed_precision` speedup, `gradient_clip` overhead, `checkpoint` overhead, `DataLoader` timing, `FSDP` overhead, `DeepSpeed` comparison, `Shampoo`/`Sophia` comparison, `Gradient accumulation` overhead, `Gradient checkpointing` overhead, `Early stopping`, `Speculative decoding` overhead, `Continuous batching`, `vLLM` compatibility, `TensorRT-LLM` comparison).


### Added
- Full decoder-only model (`GPTModel`): embedding + stacked `TransformerBlock` + final `RMSNorm` + `lm_head` with weight tying by default (`weight_tied=True`).
- `TransformerBlock`: pre-normalization (`norm` before sub-layer), residual connections (`x + Dropout(SubLayer(Norm(x)))`), `MultiHeadAttention` (manual, with optional `RoPE`), `SwiGLU` feed-forward.
- `XRFMEmbedding`: original embedding layer (`Xavier` init, weight tying design, padding support, input validation).
- `RoPE`: original rotary positional embedding (`rotate_half` + frequency rotation; configurable `base`, `max_seq_len`, `scale_factor` for future `NTK-aware` extension).
- `MultiHeadAttention`: original manual multi-head attention (`W_q`, `W_k`, `W_v`, `W_o`; scaled dot-product; masking; dropout; `RoPE` integration when configured).
- `SwiGLU`: original gated feed-forward (`W_1` gate, `W_2` value, `W_3` output; `SiLU` activation; `Xavier` init; input validation).
- `RMSNorm`: original `RMSNorm` (`gamma` learnable scale; `eps = 1e-6`; no mean subtraction; `Xavier` init).
- `tests/test_embedding.py`: `11` passing tests (shape, init, validation, config, gradient flow).
- `tests/test_attention.py`: `17` passing tests (shape, validation, masking, gradient, numerical stability, `RoPE` integration).
- `tests/test_transformer_block.py`: `13` passing tests (shape, integration, residual, pre-norm, config, input validation).
- `tests/test_model_architecture.py`: `16` passing tests (init, parameter count, weight tying, forward, gradient, numerical stability, input validation, config integration).
- `benchmark/model_forward.py`: basic benchmark framework (parameter count verification, forward pass timing, throughput measurement, future expansion reserved for `Phase 7`).
- `docs/model/ARCHITECTURE.md`: complete architecture documentation (`tensor` shapes, mathematical derivations, component diagrams, design decisions, future extension notes, performance considerations, numerical stability verification, security notes, self-review checklist).
- `DECISIONS.md`: `Phase 4` architecture entries added (`Embedding`, `RoPE`, `Multi-Head Attention`, `SwiGLU`, `RMSNorm`, `Residual`, `Weight Tying`, `Initialization`, `Numerical Stability`).

### Design Decisions
- Original manual multi-head attention (`CORE`) — full control for future `GQA`, `FlashAttention`, `Sliding Window`.
- `RoPE` (`CORE`) over `ALiBi` — relative distance property improves long-context performance; configurable (`use_rope`).
- `SwiGLU` (`CORE`) over `GELU`/`ReLU` — selective gating improves performance (`Shazeer 2020`); `MoE` replacement supported via stable interface.
- `RMSNorm` (`CORE`) over `LayerNorm` — reduced computation (`mean` omission); modern standard (`Llama 3`, `DeepSeek-V3`).
- `Pre-Norm` (`Pre-Activation`) (`CORE`) over `Post-Norm` — improves gradient flow in deep networks (`Xiong 2020`); standard modern architecture.
- Weight tying (`True` by default) (`CORE`) — reduces parameters (`vocab_size * d_model` saved); improves stability (`GPT-3` design); separate projection (`False`) available (`OPTIONAL`).
- `Xavier` initialization (`CORE`) — prevents gradient divergence (`Glorot & Bengio 2010`); `Kaiming` / `GPT-Scaled Init` deferred (`OPTIONAL`/`RESEARCH-ONLY`).
- Numerical stability (`CORE`): `softmax` scaling (`sqrt(d_head)`), masking (`masked_fill`), dropout (`p = 0.1`), `eps = 1e-6`, bounded `RoPE` rotation.

### Performance / Benchmark
- `XRFM-10M` preset (`vocab_size=50304`, `d_model=256`, `n_layers=6`, `n_heads=8`, `d_ff=1024`) produces approximately `19.2M` parameters (`embedding`: `12.9M`, `attention`: `1.6M`, `SwiGLU`: `4.7M`, `norm`: `3K`, `output`: `0` due to weight tying). Note: The preset label (`XRFM-10M`) refers to the target scale category; the actual parameter count reflects the full architecture dimensions configured in `config/config.yaml`.
- Basic benchmark (`benchmark/model_forward.py`) confirms no `NaN`/`Inf` in forward outputs; parameter count verified; timing framework established (`avg_time_ms`, `std_time_ms`, `throughput_seqs_per_sec`). Full evaluation pipeline (`multi-GPU`, `memory profiling`, `latency`) reserved for `Phase 7` (`v0.7.0`).

### Classification Updates (`DECISIONS.md`)
- `Xavier` init (`CORE`); `Kaiming` (`OPTIONAL`); `GPT-Scaled Init` (`OPTIONAL`, `Phase 8`); `Default PyTorch init` comparison (`RESEARCH-ONLY`).
- `Softmax` scaling (`CORE`); gradient clipping (`OPTIONAL`, `Phase 8`); mixed precision (`OPTIONAL`, `Phase 8`); gradient checkpointing (`RESEARCH-ONLY`, `Phase 9`).

---

## [v0.3.0] — 2026-07-24

### Added
- Dataset pipeline (`xrfm/data/loader.py`): `XRFMTextDataset` (file verification, normalization, split, chunking, tokenizer integration via `TokenizerInterface`, manifest generation `build_manifest`/`save_manifest`).
- `tests/test_data_loader.py`: `16` passing tests.
- `docs/data/DATASET_GUIDE.md`: dataset pipeline documentation.
- `DECISIONS.md`: `Phase 3` dataset strategy entry added (`TDR-002` extended).
- `CHANGELOG.md`: `v0.3.0` entry added.

### Design Decisions
- Original dataset loader (`CORE`); Hugging Face `datasets` (`OPTIONAL`, `post-v0.5.0`); streaming (`OPTIONAL`, `Phase 8`); `WebDataset` format (`RESEARCH-ONLY`).

---

## [v0.2.0] — 2026-07-24

### Added
- Original BPE tokenizer (`tokenizer/bpe.py`, `tokenizer/encode.py`, `tokenizer/decode.py`, `tokenizer/interface.py`, `tokenizer/DESIGN.md`).
- `tests/test_tokenizer_bpe.py`: `16` passing tests.
- `docs/tokenizer/README.md`: module documentation.
- `DECISIONS.md`: `Phase 2` tokenizer strategy entry added (`TDR-001`).
- `CHANGELOG.md`: `v0.2.0` entry added.

### Design Decisions
- Original BPE (`CORE`); `TokenizerInterface` stable (`CORE`); `SentencePiece` / `Unigram` / `TikToken` (`OPTIONAL`, `post-v0.5.0`).

---

## [v0.1.0] — 2026-07-24

### Added
- Project foundation (`xr-foundation-model`)
- Professional open-source repository structure
- ConfigLoader with YAML validation and dot-notation access
- Config-driven architecture supporting 10M → 1B+ without rewrites
- Git workflow with semantic version tags
- Professional documentation standards (LICENSE, CONTRIBUTING, ROADMAP)

### Design Decisions
- Pure PyTorch (no TensorFlow, no external LLM libraries for core model)
- Config-driven hyperparameters (no hard-coded values)
- Original implementation with documented conceptual references
- Clean architecture: no circular dependencies, dependency injection preferred
