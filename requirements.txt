# Runtime requirements for the XRFM API/server image (deployment/Dockerfile).
# The library core itself needs only torch/pyyaml/numpy — declared in
# pyproject.toml (`pip install xrfm`). gradio was previously listed but is
# unused by any entry point; the HF Space runs the FastAPI app.
torch>=2.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
httpx>=0.24.0
pyyaml>=6.0
numpy>=1.24.0
